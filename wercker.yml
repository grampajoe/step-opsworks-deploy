box: wercker/default
build:
  steps:
    - script:
        name: install bats
        code:
          - sudo add-apt-repository ppa:duggan/bats --yes
          - sudo apt-get update -qq
          - sudo apt-get install -qq bats
    - script:
        name: run tests
        code: bats test
